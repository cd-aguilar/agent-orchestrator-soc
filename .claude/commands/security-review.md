Review the diff for: hardcoded secrets (especially if a real threat intel
API was connected), and confirm that no new node executes destructive
actions without a human-approval step. Prioritize by severity with a
concrete remediation.
