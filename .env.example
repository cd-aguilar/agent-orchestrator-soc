# Copia este archivo a ".env" y completa tus claves reales.
# ".env" está en .gitignore: NUNCA se sube al repo.

# --- Ollama (local, no requiere key) ---
OLLAMA_HOST=http://localhost:11434

# --- Solo si migras algún agente a un proveedor cloud ---
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# --- Threat intel real (para reemplazar el mock en tools.py) ---
# VIRUSTOTAL_API_KEY=
# ABUSEIPDB_API_KEY=
# OTX_API_KEY=
